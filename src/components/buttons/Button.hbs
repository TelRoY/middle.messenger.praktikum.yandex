  {{!-- Использование:
  {{> buttons/button 
    text="Кнопка" 
    variant="primary" 
    href="/page.html"
  }} --}}

{{!-- {{#if href}}
<a href="{{href}}" class="button button-{{variant}}">
  {{text}}
</a>
{{else}}
<button type="button" class="button button-{{variant}}">
  {{text}}
</button>
{{/if}} --}}

<button 
  type="{{type}}" 
  class="component-button component-button--{{variant}} {{className}}"
  {{#if id}}id="{{id}}"{{/if}}
  {{#if disabled}}disabled{{/if}}
>
  {{text}}
</button>
